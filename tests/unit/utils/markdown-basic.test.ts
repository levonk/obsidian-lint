/**
 * Basic tests for markdown parsing utilities
 */

import { describe, it, expect } from 'bun:test';

describe('Basic markdown test', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });
});
